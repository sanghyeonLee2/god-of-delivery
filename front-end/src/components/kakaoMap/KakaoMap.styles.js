import styled from "styled-components";

export const MapWrap = styled.div`
  height: 35rem;
  position: relative;
  margin: 1.5rem 0;

  button {
    position: absolute;
    right: 5.5rem;
    bottom: 0.5rem;
    z-index: 10;
    background: none;
  }
`;
