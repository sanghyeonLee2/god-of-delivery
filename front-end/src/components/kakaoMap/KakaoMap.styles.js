import styled from "styled-components";

export const MapWrap = styled.div`
  height: 35rem;
  margin-top: 1rem;
  position: relative;

  button {
    position: absolute;
    right: 5.5rem;
    bottom: 0.5rem;
    z-index: 10;
    background: none;
  }
`;
