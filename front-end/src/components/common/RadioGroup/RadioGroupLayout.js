import styled from "styled-components";
import {elementSize, flexLayout} from "../../../assets/styles/Mixin";

export const AddressTypeWrapper = styled.div`
  ${elementSize("100%", "50px")};
  ${flexLayout("space-around", "center")};

`